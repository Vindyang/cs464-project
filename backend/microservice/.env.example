# Testing-only environment template.
# Use it only for manual integration tests that explicitly read env vars.

# Google Drive integration test inputs.
GDRIVE_OAUTH_CREDENTIALS_FILE=
GDRIVE_OAUTH_REDIRECT_URI=http://localhost:8080/api/oauth/gdrive/callback

# Optional testing overrides.
GDRIVE_FOLDER_ID=
